public with sharing class Greeter {
    // I3 session 35 — trivial comment edit to trigger a real-but-small UAT deploy for resume test
    public static String greet(String name) {
        return 'Hello, ' + name + '!';
    }
}
